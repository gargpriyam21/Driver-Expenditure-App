<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naman.namanapp">

    <uses-permission android:name="android.permission.CAMERA" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AdminActivity" />
        <activity android:name=".activities.DriverActivity" />
        <activity android:name=".activities.DriverFiles.AddExpenditureActivity" />
        <activity android:name=".activities.DriverFiles.AddIncomeActivity" />
        <activity android:name=".activities.DriverFiles.DistanceTravelledActivity" />
        <activity android:name=".activities.DriverFiles.PassbookActivity" />
        <activity android:name=".activities.AdminFiles.AdminDriverActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".AdminSignUpActivity" />
        <activity android:name=".activities.DriverFiles.CameraActivity"></activity>
    </application>

</manifest>